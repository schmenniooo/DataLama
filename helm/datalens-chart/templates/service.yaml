apiVersion: v1
kind: Service
metadata:
  name: {{ include "datalens-deployment-chart.fullname" . }}
  labels:
    {{- include "datalens-deployment-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datalens-deployment-chart.selectorLabels" . | nindent 4 }}
