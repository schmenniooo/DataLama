apiVersion: v1
kind: Service
metadata:
  name: {{ include "datalama-deployment-chart.fullname" . }}
  labels:
    {{- include "datalama-deployment-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datalama-deployment-chart.selectorLabels" . | nindent 4 }}
